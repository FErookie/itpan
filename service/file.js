const db = require('../models/index');
const  {User , File} = db.models;
//文件夹默认是私有的 文件有公开的 广场上只有文件 没有文件夹 广场上的允许设个验证码分享 先暂时啥也不用吧
async function addFile( ){

};
let fileHandle = {

};
module.exports = fileHandle;